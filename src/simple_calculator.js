let add = function(x,y){
    return (x + y);
}
let multiply = function(x,y){
    return (x * y);
}
let addMultiple = function(w,x,y){
    return (w + x + y);

}
let multiplyMultiple = function(w, x, y){
    return (w * x * y);
}
module.exports = {add, multiply, addMultiple, multiplyMultiple}